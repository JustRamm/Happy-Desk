name: app_links_linux
description: Mock app links linux package.
version: 1.0.3
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      linux:
        pluginClass: AppLinksPluginLinux
